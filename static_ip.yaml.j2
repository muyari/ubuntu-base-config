network:
  version: 2
  ethernets:
    {{ interface_name }}:
      dhcp4: no
      addresses:
        - {{ static_ip }}/{{ netmask }}
      gateway4: {{ gateway }}
      nameservers:
        addresses: {{ dns_servers }}
